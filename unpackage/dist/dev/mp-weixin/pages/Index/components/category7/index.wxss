/* flex布局 */

/* flex盒子默认排列 */
.me-fx-row {
    display: flex;
}
.me-fx-col {
    display: flex;
    flex-direction: column;
}
.me-fx-start-c {
    align-items: center;
}
.me-fx-start-end {
    align-items: flex-end;
}

/* 两端对齐 */
.me-fx-sb {
    justify-content: space-between;
}
.me-fx-sb-c {
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 20rpx;
}
.me-fx-sb-end {
    justify-content: space-between;
    align-items: flex-end;
}

/* 两端对齐，两侧有边距 */
.me-fx-sa {
    justify-content: space-around;
}
.me-fx-sa-c {
    justify-content: space-around;
    align-items: center;
}
.me-fx-sa-end {
    justify-content: space-around;
    align-items: flex-end;
}

/* 居中对齐 */
.me-fx-c {
    justify-content: center;
}
.me-fx-c-c {
    justify-content: center;
    align-items: center;
}
.me-fx-c-end {
    justify-content: center;
    align-items: flex-end;
}

/* 尾部对其 */
.me-fx-end {
    justify-content: flex-end;
}
.me-fx-end-c {
    justify-content: flex-end;
    align-items: center;
}
.me-fx-end-end {
    justify-content: flex-end;
    align-items: flex-end;
}
.me-fx-1 {
    flex: 1;
}

/* 浮动布局 */

/* 浮动元素父容器 */
.me-ft-p::after {
    content: '';
    display: block;
    clear: both;
}

/* 左浮动 */
.me-lt-ft {
    float: left;
}

/* 又浮动 */
.me-rt-ft {
    float: right;
}

/* 多余文本省略号截取样式 */
.me-full-text {
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
}
.me-full-line2 {
    -webkit-line-clamp: 2;
}

/* 定位 */
.me-pos-rel {
    position: relative;
}
.me-pos-abs {
    position: absolute;
}
.me-pos-fixed {
    position: fixed;
}
image {
    vertical-align: middle;
}
.classify-item {
    background-color: #fff;
    border-radius: 20rpx;
    overflow: hidden;
    box-shadow: 0px 0px 12px rgba(0, 0, 0, 0.16);
}
.classify-item:not(:first-child) {
    margin-top: 20rpx;
}
.classify-item .itemp-top {
    position: relative;
    padding: 20rpx 30rpx 0;
    box-sizing: border-box;
}
.itemp-top .adom-img {
    width: 41rpx;
    height: 22rpx;
}
.itemp-top .class-title {
    font-size: 32rpx;
    color: #333;
    font-weight: bold;
    margin: 0 12rpx;
}
.itemp-top .right-point {
    width: 30rpx;
    height: 30rpx;
    position: absolute;
    top: 30rpx;
    right: 20rpx;
}
.goods-scroll {
    padding: 0 30rpx;
    box-sizing: border-box;
}
.goods-list {
    padding: 30rpx 0;
}
.goods-item {
    flex-shrink: 0;
    box-shadow: 0px 0px 12px rgba(0, 0, 0, 0.09);
    width: 192rpx;
    border-radius: 20rpx;
    overflow: hidden;
}
.goods-item:not(:first-child) {
    margin-left: 20rpx;
}
.goods-img {
    width: 184rpx;
    height: 184rpx;
    background-color: #ccc;
}
.goods-item .name {
    font-size: 24rpx;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    width: 100%;
    text-align: center;
}
.now-price {
    font-size: 28rpx;
    color: #ff0000;
    font-weight: bold;
}

